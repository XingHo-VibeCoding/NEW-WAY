# 简册 · 产品需求文档（PRD）

> 版本：v1.2 ｜ 日期：2026-09-20（Day 5 收尾同步）
> 修订记录：v1.0 初稿 → v1.1 按 AI 自检结果修订（补修改/删除交互、写死分类两组、定义「近 3 个自然月」与「本月」、明确刷新行为与断网约束、补月份切换）→ v1.2 与 `TECH_DESIGN.md` 对齐（经历记录改为起止时间、新增第四个功能「导出 / 导入数据」、三组数据均加 `updatedAt`）
> 上游文档：`research.md`（Day 3 需求研究）
> 本文回答：「具体做成什么样、怎么算做完了」；**不回答**「用哪个技术框架」——技术选型在 Day 5
> 本期状态：只有需求，**不含任何代码**

---

## 一、背景与问题

**一句话**：给「同时操心钱和履历」的人，一个能看清自己底牌的地方。

**问题的本质**（引自 research.md）：市面上「记账」和「简历」是两个完全分开的品类，用户被迫在两个 App 之间来回搬数据。而这批人最关心的那个数字，两边都算不出来——因为它需要同时知道**支出**和**职业状态**。

**用户真实的处境是「看不清底牌」**：

| 看不清的 | 直接后果 |
|---|---|
| 不知道自己还能撑多久 | 花钱没底，裸辞、转行像在赌 |
| 不知道自己值多少钱 | 谈薪、跳槽全凭感觉 |

这两件事本来是一件事：**你值多少钱，决定你能撑多久；你还能撑多久，决定你敢不敢换。** 简册把这两个数字放在同一屏里。

---

## 二、目标用户（三层）

| 层级 | 是谁 | 与本产品的关系 |
|---|---|---|
| **最里层**（核心） | 正在经历职业变动、同时又得盘算钱的人 | 两件事都用，「看底牌」的直接受益者 |
| **中间层**（交叉） | 被其中一件事拉进来的人 | 两件事轮着来，谁先谁后不定 |
| **最外层**（单功能） | 只用一个功能的人 | 只记账，或只做简历 |

### 2.1 最里层：本期设计的基准用户

- **裸辞或被裁后边海投边算存款的人**——没收入、天天算存款
- **收入忽高忽低的自由职业者**——接单为生、常给客户发履历
- **工作一两年的职场新人**——一边月光，一边看机会

### 2.2 中间层：被一件事拉进来

- **简历是刚需、顺带开始记账**：应届求职季学生；手握 offer 比价的人
- **两件事轮着来**：负债还款党；斜杠青年；转行者；要盘十几年家底的转型老手；每年年底复盘的人

### 2.3 最外层：只用一个功能

- **只记账**：学生、月光族、记账爱好者、小生意个体户、攒钱打卡的人、管一家账的人
- **只做简历**：被猎头找上门的、考公考研失利转求职的、只想申请实习的

### 2.4 本期服务口径

四个功能都围绕**最里层**设计；中间层靠「记账能用 + 简历能用」自然覆盖；最外层只享受单个功能，**不做定向优化**。

> ⚠️ **口径差异（诚实标注）**：research.md 把「小生意个体户、存钱打卡党、家庭财务管理者」列为**明确排除**；本次任务清单把它们放进最外层「只用一个功能」。两处口径不一致，本文档按**本次清单**执行——含义是：它们不是本期的设计对象，只是**不阻止**他们用。**待秋鹰师确认。**

---

## 三、MVP 功能范围（必须包含的四个功能）

每个功能写清两件事：**用户动作**（人做了什么）+ **可观察结果**（做完能亲眼看见什么）。

### 功能 1 ｜记一笔

> **一句话**：三秒记下一笔收支，让你随时看清钱都去了哪。

**用户动作**

1. 在底牌页点「记一笔」→ 进入记一笔页
2. 填金额、选收/支、选分类（三项必填）；日期默认今天，不用改
3. 点「保存」
4. **改已有记录**：点流水列表里某条右侧的「编辑」→ 这条的内容填回上面的表单，按钮变成「保存修改」→ 改完点它
5. **删已有记录**：点某条右侧的「删除」→ 弹出「确认删除？此操作不可恢复」→ 点「确认」才真删，点「取消」什么都不变
6. **看别的月份**：点列表上方的月份切换（左右箭头），只看那一个月的流水

**可观察结果**

- 保存后页面提示「已记下」，输入框清空，**仍留在记一笔页**（方便连着记）
- 这笔记录立刻出现在**所选月份**的流水列表里，按日期从新到旧排；同一天的，后记的排在前面
- 如果这次填的日期**不在刚看的月份**（比如在 9 月记了一笔 8 月的支出），列表**自动跳到那条记录所在的月份**，保证你一保存就能看见它
- 列表**默认显示当前月**（电脑系统时间所在的月份）；切换月份后只显示那个月的记录
- 「本月收入 / 本月支出」的合计数字随之变化——**本月 = 当前自然月**，与列表在看哪个月无关
- 编辑一条后保存：列表里那条被更新，合计数同步变化
- 删除点「取消」→ 记录不动；点「确认」→ 那条消失，合计数同步变化
- 刷新页面（F5）后，这笔记录**还在**（页面会回到底牌页，见 8.1）

### 功能 2 ｜记一条

> **一句话**：随手记下今天做成了什么，攒成将来写简历的原料。

**用户动作**

1. 点「记一条」→ 进入记一条页
2. 填「做成了什么」（必填，一句话）；开始时间默认今天，结束时间选填（**留空 = 到现在还在做**）
3. 选填补上：公司/职位、量化结果、技能标签
4. 点「保存」
5. **改已有经历**：点列表里某条右侧的「编辑」→ 内容填回表单，按钮变成「保存修改」→ 改完点它
6. **删已有经历**：点某条右侧的「删除」→ 确认后删掉

**可观察结果**

- 保存后提示「已记下」，出现在下方的经历列表里，按结束时间从新到旧排（**还在做的排最前面**）
- 列表里每条能一眼看清：**日期 + 一句话成果 +（若有）量化结果**
- 编辑或删除后，列表和简历预览页**同步变化**
- 刷新页面后还在

### 功能 3 ｜看底牌（打开网页就落在这一页）

> **一句话**：一眼看清存款还能撑几个月，顺手把经历拼成一份简历。

**用户动作**

1. 打开网页 → 直接落在底牌页，不用点任何东西
2. 点「设置个人参数」填一次存款和月均支出（只填一次）
3. 想深究时，点数字旁边的说明，看这个数是怎么算出来的

**可观察结果**

- 页面最显眼处显示「**还能撑 ≈ 5.2 个月**」这样带单位的数字
- 同屏显示**本月收入、本月支出、本月结余**三个数字；**本月 = 电脑系统时间所在的自然月**（9 月里打开，就是 09-01～09-30）
- **数据不够时不显示 0、不报错**，而是给引导：没填存款 → 「先填一下你能动用的存款」；完全没记账也没手填月均支出 → 「先记一笔支出，或手填月均支出」
- 有记账但月份不满 3 个月时，数字旁标注「**样本不足 3 个月**」，不假装精确
- 改完一笔记录、或改完个人参数保存后，这页的数字**立即重算**（不用刷新）

### 功能 4 ｜导出 / 导入数据（v1.2 新增）

> **一句话**：把攒下的全部数据存成一个文件带走，数据丢了还能从文件搬回来。

**为什么加它**：数据只存在这台电脑的这个浏览器里，**清一次浏览器数据就永久没了**；换电脑也得能把数据搬过去。

**用户动作**

1. 在底牌页的「设置个人参数」区域，点「导出备份」
2. 浏览器把它当成一次下载 → 存下一个 `jiance-backup-2026-09-20.json` 文件
3. 换设备或数据丢了以后：点「导入备份」→ 选中那个文件 → 弹出「这会覆盖当前所有数据，确定吗？」→ 点「确认」才真导入

**可观察结果**

- 导出后，下载文件夹里多出一个 `.json` 文件；用记事本打开能看到全部记录
- 导入前**必须先确认**：点「取消」什么都不变，现有数据一条不动
- 选错文件（不是简册的备份）→ 提示「这个文件不是简册的备份」，**不导入、不损坏现有数据**
- 导入成功后，底牌页数字和两个列表**全部变成备份里的内容**
- **它是手动的**：不会自动备份、不会自动同步

**明确不做**：云备份、自动定时导出、多份备份管理、**备份文件加密**（备份是明文的，用记事本就能看懂）

---

## 四、页面结构（四个页面）

**形态**：一个单页网页（`index.html`），双击打开即用。四个页面用「切换显示」实现，不跳转新网址、不需要服务器。

| # | 页面 | 怎么到这里 | 这页有什么 | 关键动作 |
|---|---|---|---|---|
| 1 | **底牌页**（默认首页） | 打开网页直接落在这一页 | 「还能撑 ≈ X 个月」大数字；本月收入/支出/结余；个人参数入口（含**导出备份 / 导入备份**两个小按钮）；两个大按钮（记一笔 / 记一条） | 看数字、点按钮、改参数、导出 / 导入备份 |
| 2 | **记一笔页** | 底牌页点「记一笔」 | 输入表单 + 所选月份的流水列表（**默认当前月**，可左右切换月份） | 新增、修改、删除一笔收支；切换月份 |
| 3 | **记一条页** | 底牌页点「记一条」 | 输入表单 + 经历列表（按结束时间从新到旧） | 新增、修改、删除一条经历 |
| 4 | **简历预览页** | 从任意页面点导航「简历预览」 | 攒下的经历按时间排成的一份简历 | 看；用浏览器打印/另存为 PDF |

**导航**：一条固定的导航条（四个页面互相能直达），任何页面都能一键回底牌页。

**导出 / 导入放哪**：它不单独占一个页面，放在**底牌页的「设置个人参数」区域**里——它是低频操作（可能一个月才点一次），不该挤进导航条。

> ⚠️ 入口位置是**默认选择**（原因是「数据管理属于设置」），待秋鹰师确认；想放在别处（比如导航条最右侧）只要改这一处。

**页面流转**（一图说清）：底牌页是中心，记一笔、记一条都从它出发；简历预览页是「出口」，随时可看。

---

## 五、数据字段

三组数据，**全部存在你自己电脑的浏览器里**（localStorage），不联网、不上传。

### 5.1 收支记录

| 字段 | 类型 | 必填 | 说明与示例 |
|---|---|---|---|
| `id` | 文本 | 系统生成 | 唯一编号，用来定位这一条 |
| `date` | 日期 | 是 | 默认今天；例 `2026-09-19` |
| `amount` | 数字 | 是 | 金额，**正数**，最多两位小数；例 `38.50` |
| `type` | 二选一 | 是 | `收入` / `支出` |
| `category` | 固定选项 | 是 | 本期**固定分类**，不分一级二级；**按收/支分两组，选项写死**：<br>支出＝餐饮 / 交通 / 房租 / 购物 / 医疗 / 娱乐 / 其他<br>收入＝工资 / 兼职 / 理财收益 / 其他<br>选「收入」时下拉里只出现收入组 |
| `note` | 文本 | 否 | 不超过 50 字；例 `楼下快餐` |
| `createdAt` | 时间 | 系统生成 | 记录创建时间 |
| `updatedAt` | 时间 | 系统生成 | **最后修改时间；新增和每次编辑都会刷新** |

### 5.2 经历记录

| 字段 | 类型 | 必填 | 说明与示例 |
|---|---|---|---|
| `id` | 文本 | 系统生成 | 唯一编号 |
| `startDate` | 日期 | 是 | **开始时间**，默认今天；例 `2025-03-01` |
| `endDate` | 日期 | 否 | **结束时间**；**不填 = 到现在还在做** |
| `content` | 文本 | 是 | 做成了什么，一句话；例 `把首页加载从 5 秒压到 2 秒` |
| `org` | 文本 | 否 | 公司 / 团队 |
| `role` | 文本 | 否 | 职位 |
| `result` | 文本 | 否 | 量化结果；例 `响应快 3 秒` |
| `tags` | 文本（可多个） | 否 | 技能 / 关键词；例 `性能优化`、`React` |
| `createdAt` | 时间 | 系统生成 | 创建时间 |
| `updatedAt` | 时间 | 系统生成 | 最后修改时间 |

**简历预览页的排序规则**（起止时间带来的新规则）：

1. 先按 `endDate` 从晚到早排；
2. `endDate` 为空的（**还在做的**），**排在最前面**；
3. 两条 `endDate` 相同的，按 `startDate` 从晚到早排。

### 5.3 个人参数（算「能撑几个月」要用）

| 字段 | 类型 | 必填 | 说明与示例 |
|---|---|---|---|
| `savings` | 数字 | 是 | 可动用的钱（**能立刻拿出来花的**，不含定期、不含借出去的钱）；例 `30000` |
| `monthlyExpense` | 数字 | 否 | 手填的月均支出；**不填**就用近 3 个月记账自动算；例 `5800` |
| `updatedAt` | 时间 | 系统生成 | 最后修改时间 |

**计算口径（本期只有这一条）**：

```
还能撑几个月 = 可动用存款 ÷ 月均支出
```

- **「近 3 个自然月」的定义**：当前月 + 前 2 个月（例：9 月打开，就是 7 月、8 月、9 月三个月）
- **月均支出怎么取**（按顺序判断，取到哪条就停）：
  1. 这 3 个月里有支出记录 → 月均支出 = 3 个月的支出合计 ÷ 3
  2. 有支出记录但**不满 3 个自然月** → 月均支出 = 有支出月份的合计 ÷ 有支出的月份数，并在数字旁标注「**样本不足 3 个月**」
  3. 一条支出记录都没有 → 用你手填的 `monthlyExpense`
  4. 手填也没有 → 不显示数字，显示引导文案
- 「能撑几个月」这个数字，**只有填了可动用存款（`savings`）才会出现**
- 展示时**保留一位小数**，例 `≈ 5.2 个月`

---

## 六、本期不做（砍了什么、为什么砍）

### 6.1 判断标准（三条线，砍任何一条都对得上）

1. **技术装不下**——需要后端、数据库、账号体系，26 天学不完
2. **工程量是公司级的**——需要第三方接口、资质、商务合作
3. **对核心假设没帮助**——做出来也验证不了「把收入和职业放在一起看」这件事

### 6.2 重点讲的三条（今天主题）

| 砍掉的功能 | 归哪条线 | 为什么砍 |
|---|---|---|
| **注册 / 登录、账号体系** | 技术装不下 | 要后端 + 数据库 + 密码安全，26 天学不完；数据只在本机反而更快上线 |
| **支付宝 / 微信 / 银行账单自动导入** | 工程量是公司级 | 要对接第三方接口、签名、合规审核，是公司团队几个月的活；本期手记一笔 3 秒就够验证 |
| **AI 生成简历文案、AI 简历评分** | 对核心假设没帮助 | 要接付费大模型 API 和管理密钥；而且它优化的是「文案好不好」，跟「我的钱能撑多久」这个核心假设毫无关系 |

### 6.3 其余不做的功能（沿用 research.md，不展开）

| # | 不做的功能 | 为什么 |
|---|---|---|
| 1 | 云端同步、多设备 | 需要后端与账号（见上表） |
| 2 | 简历模板市场、多套模板 | 是超级简历的护城河，不是我们的；本期只做 1 套 |
| 3 | 真正的 PDF / Word 导出 | 用浏览器「打印为 PDF」替代，效果够用 |
| 4 | 手机 App（iOS / 安卓）、小程序 | 要学打包、上架、审核，全部超范围 |
| 5 | 多人 / 家庭共享账本 | 需要账号和同步 |
| 6 | 理财、投资、基金、股票 | 钱迹的明确边界，也是合规红线 |
| 7 | 招聘平台对接、一键投递 | 需要企业资质和商务合作 |
| 8 | 数据分享海报、社交裂变 | 核心体验还没验证，先不做增长 |
| 9 | 高级图表（桑基图、热力图、自定义看板） | 一张图就够验证假设 |
| 10 | 多币种、汇率换算 | 目标用户是国内求职人群，无实际场景 |
| 11 | 数据加密、密码保护 | 数据只在本机、不上传，风险等级低 |

### 6.4 明确列为「后续」的三项

| 顺延的功能 | 为什么顺延（不是忘了做） |
|---|---|
| **求职进度管理**（投递记录、面试进度、offer 管理） | 本期先解决「看清底牌」；进度管理是另一件事，现在做会把 MVP 撑大 |
| **收入折线 × 职业节点的时间轴图** | 数据攒够之前画不出有意义的图；本期先用「底牌页数字 + 简历按时间排」验证 |
| **跳槽对比卡**（一年多多少 / 提前多久还清） | 属于「职业变动决策」，本期先做「看清现状」这一步 |

---

## 七、技术前提与本期边界

| 项 | 本期口径 |
|---|---|
| 技术形态 | **纯前端**：HTML + CSS + JavaScript，单个 `index.html`，双击打开即用 |
| 数据存储 | 浏览器自带的 `localStorage`，不联网、不注册、不上传 |
| 为什么这么定 | 第 1 周的目标是走通「本地写代码 → 提交到 GitHub」，不是造平台 |
| 代码 | **本文档不含代码**；技术选型在 Day 5，写代码在更后面的天（以当天清单为准） |
| 界面 | 自己写样式，只做 1 套，不做主题切换 |
| 外部依赖 | **不引用任何外部资源**（在线字体、图标库、CDN 脚本），断网也能正常打开 |
| 刷新行为 | 刷新（F5）后**回到底牌页**；已保存的数据不丢，正在填、没保存的内容会丢 |

**粗排期**（沿用 research.md，每天具体做什么以当天清单为准）：

| 阶段 | 产出 |
|---|---|
| 第 2 周 | 记一笔 + 流水列表能用（能记能看） |
| 第 3 周 | 记一条 + 数据存取打通 |
| 第 4 周 | 底牌页（能撑几个月）+ 简历预览 |
| 最后 2–3 天 | 样式统一、写 README、发布 |

**卡住时的降级（写死在这里，不要临场慌）**：如果时间不够，优先级为
底牌页（数字）→ 记一笔 → 简历预览 → 记一条。前两个保住，核心假设就能被验证。

---

## 八、验收标准（可逐条检查）

> 每一条都是**你亲手点一下就能判断真假**的动作。全部打勾 = 本期需求完成。

### 8.1 全局

- [ ] 双击 `index.html`，浏览器能打开，**不报错、不需要任何安装**
- [ ] 打开后**先落在底牌页**（不是空页、不是记账列表）
- [ ] 导航条上能看到四个入口：底牌页、记一笔、记一条、简历预览
- [ ] 从任意页面都能一键回到**底牌页**
- [ ] 刷新（F5）后**回到底牌页**，已保存的数据不丢（正在填、还没保存的内容会丢，属预期行为）

### 8.2 底牌页

- [ ] 显示「还能撑 ≈ X 个月」，**带单位**，保留一位小数
- [ ] 同屏显示**本月收入、本月支出、本月结余**三个数字
- [ ] 「本月」就是电脑系统时间所在的自然月（9 月打开，只统计 9 月的记录）
- [ ] 本月结余 = 本月收入 − 本月支出（数字能对上）
- [ ] 没填「可动用存款」时，显示「先填一下你能动用的存款」这类引导，**不显示 0、不显示 NaN、不报错**
- [ ] 完全没有支出记录、也没手填月均支出时，显示引导文案，不猜数字
- [ ] 有记账但不满 3 个自然月时，数字旁能看到「**样本不足 3 个月**」标注
- [ ] 改一笔收支记录、或改完个人参数保存后回到这页，「还能撑几个月」**跟着变**（不用刷新）
- [ ] 能从这里一键进入记一笔页和记一条页

### 8.3 记一笔

- [ ] 能填：日期（默认今天）、金额、收/支、分类、备注
- [ ] 选「收入」时分类下拉里只有收入类（工资 / 兼职 / 理财收益 / 其他）；选「支出」时只有支出类
- [ ] 金额填 0 或负数时**不能保存**，并给出提示
- [ ] 必填项没填时不能保存，并指出缺哪一项
- [ ] 保存后提示「已记下」，输入框清空，**仍留在记一笔页**
- [ ] 新记录出现在流水列表里，按日期从新到旧
- [ ] 列表**默认显示当前月**；点左右箭头切到别的月份，只看到那个月的记录
- [ ] 在 9 月记一笔日期为 8 月的支出，保存后列表**自动跳到 8 月**，这笔能立刻看见
- [ ] 点某条右侧「编辑」，内容填回表单、按钮变成「保存修改」；保存后那条被更新，合计数同步变化
- [ ] 点某条右侧「删除」会先弹确认：点「取消」记录不动，点「确认」才消失，合计数同步变化
- [ ] 同一天记了两笔，后记的排在前面
- [ ] 刷新页面后以上记录**都还在**

### 8.4 记一条

- [ ] 能填：**开始时间**（默认今天）、**结束时间**（选填，留空 = 到现在还在做）、做成了什么（必填）、公司/职位（选填）、量化结果（选填）、技能标签（选填）
- [ ] 「做成了什么」为空时不能保存，并给出提示
- [ ] 保存后提示「已记下」，出现在经历列表里，按 `endDate` 从新到旧排（**还在做的排最前面**）
- [ ] 列表里每条能看清：**起止时间（还在做的显示「至今」）+ 一句话成果 +（若有）量化结果**
- [ ] 点某条经历右侧「编辑」，内容填回表单、按钮变「保存修改」；保存后列表和简历预览页同步更新
- [ ] 点某条经历右侧「删除」，确认后经历消失，简历预览页也跟着少一条
- [ ] 刷新页面后经历**还在**

### 8.5 简历预览页

- [ ] 攒下的经历**按结束时间从新到旧**排成一份简历；**还在做的（没填结束时间）排在最前面**
- [ ] 每条经历显示起止时间（还在做的显示「至今」）、内容，有公司/职位/量化结果/标签的也一并显示
- [ ] 一条经历都没记时，显示引导（「先去记一条你的成果」），不是空白页
- [ ] 能用浏览器自带的「打印 / 另存为 PDF」打出这份简历（本期不自写 PDF 功能）

### 8.6 数据与边界

- [ ] 收支记录和经历记录**互不干扰**：删掉一笔钱，经历还在
- [ ] 数据只存在本机浏览器，**全程没有联网请求**；断网（拔网线 / 关 WiFi）也能照常打开和记录
- [ ] 页面没有引用任何外部资源（在线字体、图标库、CDN）——断网后样式和图形仍正常显示
- [ ] 关闭浏览器再打开，数据**还在**
- [ ] 页面上**没有**注册、登录、云同步这些入口（说明真的砍掉了）

### 8.7 导出 / 导入数据（v1.2 新增）

- [ ] 底牌页的「设置个人参数」区域能看到「导出备份」「导入备份」两个按钮
- [ ] 点「导出备份」→ 下载文件夹里多出一个 `jiance-backup-YYYY-MM-DD.json`
- [ ] 用记事本打开这个文件，能看到全部收支记录和经历记录（**明文、不加密**）
- [ ] 点「导入备份」→ 选中一个**不是**简册备份的文件 → 提示「这个文件不是简册的备份」，且**现有数据一条没变**
- [ ] 点「导入备份」→ 选中正确的备份 → 弹出「这会覆盖当前所有数据，确定吗？」；点「取消」什么都不变
- [ ] 再点一次、这次点「确认」→ 底牌页数字和两个列表全部变成备份里的内容
- [ ] 备份文件是**手动导出**的：不会自动生成、不会自动上传

---

## 九、与 research.md 的差异（诚实标注）

> 本文档按**本次任务清单**写，与 Day 3 的 research.md 有几处口径不同。列出来是为了以后不打架。

| # | research.md 的写法 | 本文档口径 | 原因 |
|---|---|---|---|
| 1 | MVP 含模块 ⑥「导出」（简历复制成文本 / 打印 PDF） | 不在必须包含的功能内；简历预览页保留浏览器打印。**注意区分**：v1.2 新增的「功能 4 导出 / 导入」是**数据备份**（`.json`），与这里的「导出简历」是两件事 | 本次清单只点名三个功能；数据备份是 Day 5 另行拍板 |
| 2 | ④ 联动时间轴（收入折线 + 职业节点）是**核心** | **不在本期 MVP**，列为后续 | 按本次清单 |
| 3 | ⑤ 三个结论卡（能撑多久 / 每月缺口 / 跳槽对比） | 只保留「能撑多久」+ 本月收支；跳槽对比列后续 | 按本次清单 |
| 4 | 明确排除小生意个体户、存钱打卡党、家庭财务管理者 | 归入最外层「只用一个功能」用户 | 按本次清单，**口径不一致，待确认** |
| 5 | 档案条目 = 公司、职位、起止月份、月薪 | 改为「记一条」成果记录，公司/职位为选填，月薪不进本期 | 按本次清单「随手记下今天做成了什么」 |

---

## 十、待确认 / 风险

| # | 事项 | 说明 |
|---|---|---|
| 1 | 「能撑多久」用户是否真的在意 | research.md 第 8 条：**未做用户访谈**，是最该被证伪的假设 |
| 2 | 三层用户的实际优先级 | research.md 第 7 条：原始画像表没写优先级判定依据 |
| 3 | 固定分类清单是否够用 | 本期不分层级，实测后可能要补 |
| 4 | 简历预览的「够」到什么程度 | 本期只做「按时间排列 + 打印」，不做模板与排版美化 |
| 5 | 导出 / 导入的入口放在底牌页「设置个人参数」区 | **入口位置是默认选择**，待确认；想放别处（如导航条右侧）改 PRD 第四节一处即可 |
| 6 | 备份文件明文、不加密 | **已定**（2026-09-20）：不加密。数据只在本机，加密反而多出「忘记密码 = 自己的数据打不开」的风险 |

---

*本文档由 Day 4 任务产出；v1.2 于 Day 5 收尾时与 `TECH_DESIGN.md` 对齐。不写代码，不做技术选型。*
